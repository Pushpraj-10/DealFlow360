PORT = 8001
CORS_ORIGIN = http://localhost:3000

MONGODB_URI = mongodb+srv://<user>:<password>@<cluster-host>/

ACCESS_TOKEN_SECRET = <a long random string>

FRONTEND_URL = http://localhost:3000

# Optional: signup approve/reject notification emails are skipped if SMTP_HOST is unset.
SMTP_HOST =
SMTP_PORT = 587
SMTP_SECURE = false
SMTP_USER =
SMTP_PASS =
SMTP_FROM = DealFlow360 <no-reply@dealflow360.test>
